[global]
daemonize = no

[www]
user = www-data
group = www-data
listen = /run/php/php7.4-fpm.sock  # Use UNIX socket instead of TCP
listen.owner = www-data
listen.group = www-data
pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 2
pm.max_spare_servers = 5
